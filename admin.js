js/categories.js        ← nouveau (manquait dans le projet)
js/admin-products.js    ← nouveau (CRUD produits)
js/firebase-config.js   ← déjà chez vous, inclus ici pour référence
js/admin-auth.js        ← déjà chez vous, inclus ici pour référence
admin/login.html        ← nouveau
admin/dashboard.html    ← nouveau
admin/produits.html     ← nouveau
admin/produit-form.html ← nouveau
firebase/firestore.rules ← nouveau (règles de sécurité recommandées)
